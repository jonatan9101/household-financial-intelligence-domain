services:
  postgres:
    image: postgres:16
    container_name: hfi-postgres
    environment:
      POSTGRES_DB: hfi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - hfi-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d hfi"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  hfi-postgres-data:
